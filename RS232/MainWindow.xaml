<Window x:Class="RS232.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="470.4" Width="558.6" Loaded="Window_Loaded" Closed="Window_Closed">
    <Grid>
        <ComboBox x:Name="comBox" HorizontalAlignment="Left" Margin="421,10,0,0" VerticalAlignment="Top" Width="120"/>
        <ComboBox x:Name="baudBox" HorizontalAlignment="Left" Margin="421,37,0,0" VerticalAlignment="Top" Width="120"/>
        <Button x:Name="btnOpen" Content="Open" HorizontalAlignment="Left" Margin="451,84,0,0" VerticalAlignment="Top" Width="75" Click="btnOpen_Click"/>
        <ListView x:Name="InBytes" HorizontalAlignment="Left" Height="100" Margin="51,296,0,0" VerticalAlignment="Top" Width="100">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
